# Hotel Employee Onboarding System - Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control

# ====================
# Database Configuration
# ====================
DATABASE_URL=postgresql://user:password@host:port/database
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# ====================
# Google Document AI Configuration (Primary OCR Service)
# ====================
# Google Cloud Project ID containing the Document AI processor
GOOGLE_PROJECT_ID=your-google-project-id

# Document AI Processor ID (Form Parser or Custom Processor)
GOOGLE_PROCESSOR_ID=your-processor-id

# Processor Location (usually 'us' or 'eu')
GOOGLE_PROCESSOR_LOCATION=us

# Path to Google Service Account JSON file (local development)
# For production, use GOOGLE_CREDENTIALS_BASE64 instead
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# Base64-encoded Google Service Account JSON (for Heroku/production)
# Generate with: base64 -i your-service-account.json
# GOOGLE_CREDENTIALS_BASE64=base64-encoded-json-here

# ====================
# Groq API Configuration (Fallback OCR Service)
# ====================
GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=llama-3.2-90b-vision-preview
GROQ_TEMPERATURE=0.2
GROQ_MAX_TOKENS=4096

# ====================
# Authentication & Security
# ====================
# Secret key for JWT token signing (generate with: openssl rand -hex 32)
JWT_SECRET_KEY=your-secret-key-min-32-chars

# JWT token expiration in hours
JWT_ACCESS_TOKEN_EXPIRE_HOURS=24

# Encryption key for sensitive data (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-encryption-key-min-32-chars

# ====================
# Email Configuration (SMTP)
# ====================
# SMTP server settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USERNAME=your-email@gmail.com
# For Gmail, use App Password (not regular password)
SMTP_PASSWORD=your-app-specific-password

# Email sender information
FROM_EMAIL=noreply@yourcompany.com
FROM_NAME=Hotel Onboarding System

# ====================
# Application Configuration
# ====================
# Frontend URL for emails, QR codes, and CORS
FRONTEND_URL=https://your-frontend-domain.com

# Environment (development, staging, production)
ENVIRONMENT=development

# Debug mode (true/false) - set to false in production
DEBUG=false

# ====================
# Optional Services
# ====================
# Redis URL for caching (optional)
# REDIS_URL=redis://localhost:6379

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ====================
# Notes for Deployment
# ====================
# Heroku Deployment:
# - Use heroku config:set to set each variable
# - For Google credentials, use GOOGLE_CREDENTIALS_BASE64
# - Ensure DATABASE_URL is automatically set by Heroku Postgres addon
#
# Vercel Frontend:
# - Only needs VITE_API_URL and VITE_APP_URL
# - Set in Vercel dashboard or .env.production
#
# Security:
# - Never commit actual values to git
# - Rotate keys regularly
# - Use strong, unique passwords
# - Enable 2FA on all service accounts